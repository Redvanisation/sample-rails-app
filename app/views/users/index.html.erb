<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
    <!--<% @users.each do |user| %>
        <%= render 'users/user', user: user %>
    <% end %>-->
    <%= render @users %>
</ul>

<%= will_paginate %>